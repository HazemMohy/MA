{
    "dataset_choice": "2x",
    "learning_rate": 1e-5,
    "max_epochs": 10,
    "batch_size": 1,
    "val_interval": 1,
    "comments": {
        "batch_size_conditions": {
            "max_epochs_100": "Batch_size = 1 or 2",
            "max_epochs_1000": "Batch_size = 4 or 8 (larger batch sizes provide more stable gradient estimates)"
        }
    }
}

