from PIL import Image

# Replace 'your_image.tiff' with the path to your TIFF file
image_path = 'your_image.tiff'

# Open the TIFF file
with Image.open(image_path) as img:
    width, height = img.size

print(f"Width: {width}px, Height: {height}px")